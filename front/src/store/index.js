import Vue from 'vue'
import Vuex from 'vuex'
import nodesCounter from './nodesCounter'

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    nodesCounter,
  }
})
